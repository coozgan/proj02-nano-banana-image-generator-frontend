# Backend API base URL (empty = use Vite proxy in dev)
VITE_API_BASE_URL=

# Shared internal access token (must match INTERNAL_TOKEN on the server)
VITE_INTERNAL_TOKEN=change-me-to-a-long-random-string
